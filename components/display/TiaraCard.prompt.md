Surface/panel container with optional titled header.

```jsx
<TiaraCard title="Righe ordine" headerActions={<TiaraButton size="sm" variant="ghost">Aggiungi</TiaraButton>}>
  …table…
</TiaraCard>
```

Use `flat` to drop the shadow inside already-elevated contexts.
